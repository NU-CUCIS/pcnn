/*
 * Copyright (C) 2020, Northwestern University
 * See COPYRIGHT notice in top-level directory.
 */
int pcnn_frame_run(struct feeder_t *feeder,
                   struct model_t *model,
                   struct param_t *param,
                   struct comm_queue_t *queue);
